import React from "react";
import ReactLoading from "react-loading";

const Loading = () => (
  <ReactLoading type="spinningBubbles" color="blue" height={200} width={75} />
);

export default Loading;
